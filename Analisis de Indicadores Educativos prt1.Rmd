---
title: "Análisis Exploratorio de Datos: Indicadores Educativos Municipales Colombia 2024"
author: 
  - "David Santiago Lugo Piñeros - Cod:20222020180"
  - "Luisa Fernández Velasco López - Cod:20212020106"
  - "Jairo Arturo Barrera Mosquera - Cod:20222020142"
  - "Gabriela Martinez Silva - Cod:20231020205"
date: "`r Sys.Date()`"
output: 
  html_document:
    toc: true
    toc_float: true
    toc_depth: 3
    theme: flatly
    highlight: tango
    code_folding: show    
    df_print: paged
editor_options: 
  markdown: 
    wrap: 72
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
echo = TRUE,
warning = FALSE, 
message = FALSE,
fig.align = "center", 
fig.width = 8, 
fig.height = 6,
dpi = 300,
out.width = "85%"
)
#Paquetes que se usan en el documento
paquetes <- c("ggplot2", "dplyr", "tidyr", "reshape2", "knitr", "corrplot")
faltantes <- paquetes[!vapply(paquetes, requireNamespace, logical(1), quietly = TRUE)]
if (length(faltantes) > 0) install.packages(faltantes)
#Librerias que se usan en el documento 
library(readr)
library(dplyr)
library(ggplot2)
library(corrplot)
library(knitr)
library(DT)
# se define a datos como la variable donde se guardara la ruta del dataset
datos <- read.csv(file.path("EDUCACION.csv"),
check.names = FALSE,
stringsAsFactors = FALSE
)
```

# Introducción

El presente documento tiene como objetivo revisar un conjunto de datos
que contiene los indicadores educativos a nivel municipal en Colombia
correspondientes al año 2024. Este dataset esta compuesto por 15.707
filas y 41variables, fue obtenido a través de Kaggle y recopila
información oficial suministrada por el Ministerio de Educación
Nacional.

Para llevar a cabo la revisio de manera óptima se iniciara con la
clasificación de los datos en variables numéricas y categóricas. Esto
nos permitirá definir el tratamiento adecuado para cada tipo de
información.

Se emplearán histogramas para visualizar cómo se distribuyen los datos
numéricos de forma individual. Asimismo, se utilizarán diagramas de caja
(boxplots) con las variables numéricas normalizadas, lo que nos ayudará
a identificar la mediana, los cuartiles y los valores atípicos. Por otro
lado, las variables categóricas se utilizarán para organizar y comparar
la información mediante frecuencias.

Posteriormente se llevarán a cabo cruces entre variables para
identificar relaciones específicas mediante gráficos de dispersión, lo
que nos permitirá comprender el comportamiento conjunto de dos
indicadores.

Luego Se construirá una matriz de correlación que estará representada en
un mapa de calor (heatmap) para observar matemáticamente la relación
simultánea entre todas las variables numéricas. Esta herramienta será
fundamental para generar hipótesis sobre qué factores educativos crecen
o disminuyen en conjunto.

# Descripción del conjunto de datos

```{r dimensiones}
dim(datos) 
```

El dataset esta compuesto originalmente por 15.707 registros y 41
variables. Su propósito es reunir información relacionada con distintos
indicadores educativos de los municipios de Colombia durante el año 2024
para una población de un rango de edad de 5 y 16 años, proporcionando
una perspectiva territorial sobre las condiciones y características del
sector educativo. Cada registro permite asociar diferentes variables e
indicadores con un municipio determinado. A continuación, se describen
las variables del conjunto de datos estructuradas por categoría
metodológica.

```{r estructura}
str(datos)
```

#### 1. Variables de Identificación Geográfica

| Variable | Tipo Original | Descripción |
|:---|:---|:---|
| `CÓDIGO_MUNICIPIO` | Entero | Código DANE único del municipio |
| `MUNICIPIO` | Texto | Nombre del municipio |
| `CÓDIGO_DEPARTAMENTO` | Entero | Código DANE del departamento |
| `DEPARTAMENTO` | Texto | Nombre del departamento |
| `CÓDIGO_ETC` | Entero | Código de la Entidad Territorial Certificada |
| `ETC` | Texto | Nombre de la Entidad Territorial Certificada |

#### 2. Variables Demográficas

| Variable | Clasificación | Tipo Original | Descripción |
|:------------------|-----------------|:------------------|:------------------|
| `POBLACIÓN_5_16` | **Numérica** | Entero | Población total entre 5 y 16 años en el municipio |

#### 3. Variables de Matrícula y Cobertura

| Variable | Tipo Original | Clasificación | Descripción |
|:-----------------|:-----------------|:-----------------|:-----------------|
| `TASA_MATRICULACIÓN_5_16` | Float | **Numérica** | \% de población de 5-16 años matriculada en el sistema educativo |
| `COBERTURA_NETA` | Float | **Numérica** | \% de población en edad escolar matriculada en el nivel correspondiente (general) |
| `COBERTURA_NETA_TRANSICIÓN` | Float | **Numérica** | Cobertura neta en nivel de transición (preescolar) |
| `COBERTURA_NETA_PRIMARIA` | Float | **Numérica** | Cobertura neta en educación primaria |
| `COBERTURA_NETA_SECUNDARIA` | Float | **Numérica** | Cobertura neta en educación secundaria |
| `COBERTURA_NETA_MEDIA` | Float | **Numérica** | Cobertura neta en educación media |
| `COBERTURA_BRUTA` | Float | **Numérica** | \% de población total matriculada, sin importar edad (general) |
| `COBERTURA_BRUTA_TRANSICIÓN` | Float | **Numérica** | Cobertura bruta en transición |
| `COBERTURA_BRUTA_PRIMARIA` | Float | **Numérica** | Cobertura bruta en primaria |
| `COBERTURA_BRUTA_SECUNDARIA` | Float | **Numérica** | Cobertura bruta en secundaria |
| `COBERTURA_BRUTA_MEDIA` | Float | **Numérica** | Cobertura bruta en media |

#### 4. Variables de eficiencia

| Variable | Tipo Original | Clasificación | Descripción |
|:-----------------|:-----------------|:-----------------|:-----------------|
| `DESERCIÓN` | Float | **Categórica** | Tasa general de deserción escolar (abandono del sistema) |
| `DESERCIÓN_TRANSICIÓN` | Float | **Categórica** | Deserción en nivel de transición |
| `DESERCIÓN_PRIMARIA` | Float | **Categórica** | Deserción en educación primaria |
| `DESERCIÓN_SECUNDARIA` | Float | **Categórica** | Deserción en educación secundaria |
| `DESERCIÓN_MEDIA` | Float | **Categórica** | Deserción en educación media (variable clave) |
| `APROBACIÓN` | Float | **Categórica** | Tasa general de aprobación (estudiantes que pasan de grado) |
| `APROBACIÓN_TRANSICIÓN` | Float | **Categórica** | Aprobación en transición |
| `APROBACIÓN_PRIMARIA` | Float | **Categórica** | Aprobación en primaria |
| `APROBACIÓN_SECUNDARIA` | Float | **Categórica** | Aprobación en secundaria |
| `APROBACIÓN_MEDIA` | Float | **Categórica** | Aprobación en educación media (variable objetivo) |
| `REPROBACIÓN` | Float | **Numérica** | Tasa general de reprobación (estudiantes que pierden el grado) |
| `REPROBACIÓN_TRANSICIÓN` | Float | **Numérica** | Reprobación en transición |
| `REPROBACIÓN_PRIMARIA` | Float | **Numérica** | Reprobación en primaria |
| `REPROBACIÓN_SECUNDARIA` | Float | **Numérica** | Reprobación en secundaria |
| `REPROBACIÓN_MEDIA` | Float | **Numérica** | Reprobación en educación media |
| `REPITENCIA` | Float | **Numérica** | Tasa general de repitencia (estudiantes que cursan nuevamente el mismo grado) |
| `REPITENCIA_TRANSICIÓN` | Float | **Numérica** | Repitencia en transición |
| `REPITENCIA_PRIMARIA` | Float | **Numérica** | Repitencia en primaria |
| `REPITENCIA_SECUNDARIA` | Float | **Numérica** | Repitencia en secundaria |
| `REPITENCIA_MEDIA` | Float | **Numérica** | Repitencia en educación media |

#### 5. Variables de Infraestructura Educativa

| Variable | Tipo Original | Clasificación | Descripción |
|:-----------------|:-----------------|:-----------------|:-----------------|
| `TAMAÑO_PROMEDIO_DE_GRUPO` | Float | **Numérica** | Promedio de estudiantes por grupo académico |
| `SEDES_CONECTADAS_A_INTERNET` | Float | **Categórica** | Proporción de sedes educativas con conexión a internet |

# Carga y Limpieza

Se inicia el proceso de limpieza del conjunto de datos con el fin de
establecer un flujo de trabajo estable que garantiza la correcta
ejecución de las visualizaciones y el cálculo preciso de los
estadísticos para cada variable facilitando así la comprensión del
comportamiento de la información en el sector educativo.

## Tratamiento de datos faltantes

Primeramente se hace la depuración, se llevó a cabo la identificación y
sustitución de aquellos espacios en blanco presentes en el data set por
el valor NA. Esta transformación resulta fundamental, ya que permite que
los datos sean interpretados de manera adecuada por las herramientas de
análisis, evitando que los campos vacíos sean tratados como cadenas de
texto o como valores válidos dentro de las variables.

```{r}
datos[datos == "" | datos == " "] <- NA
```

Procedemos a revisar cual es el porcentaje de datos faltantes por
columna para saber con que datos nos sirven para que sea posible
analizar mejor el conjunto de datos.

```{r}
porcentaje_nulos <- round(colMeans(is.na(datos)) * 100, 2) 

```

Luego de revisar que los datos NA no superan mas del 1% en las filas y
que el mayor problema esta en las variables TAMAÑO_PROMEDIO_DE_GRUPO y
SEDES_CONECTADAS_A_INTERNET procedemos con el siguiente paso.

## Exclusión y selección de variables para el análisis

Se ha decidido excluir la variable AÑO ya que se tiene conocimiento de
que todos los datos corresponden al año 2024 por esa razón la variable
no aporta información relevante para la comprensión del conjunto de
datos.

Asimismo, las variables CODIGO_MUNICIPIO y CODIGO_DEPARTAMENTO no serán
consideradas ya que la información que proporcionan puede obtenerse a
través de los nombres de los municipios y departamentos. Estos códigos
actúan únicamente como identificadores y no resultan relevantes para los
objetivos de este estudio.

También se omitirán las variables ETC y CODIGO_ETC, puesto que su
contenido se limita a indicar la entidad territorial que administra los
servicios educativos, lo cual no es pertinente para el análisis que se
pretende realizar.

Finalmente, las variables TAMAÑO_PROMEDIO_DE_GRUPO y
SEDES_CONECTADAS_A_INTERNET presentan aproximadamente un 50% de valores
vacíos. Se ha determinado que deben eliminarse en su totalidad ya que
cualquier intento de imputación implicaría inventar la mitad de la
información lo que restaría validez.

```{r}
variables_a_excluir <- c(
  "AÑO", 
  "CÓDIGO_MUNICIPIO", 
  "CÓDIGO_DEPARTAMENTO", 
  "ETC", 
  "CÓDIGO_ETC", 
  "TAMAÑO_PROMEDIO_DE_GRUPO", 
  "SEDES_CONECTADAS_A_INTERNET"
)

datos_excluidos <- datos %>%
  select(-any_of(variables_a_excluir))
```

## Transformación de datos

Para garantizar un flujo de trabajo que sea fluido y evitar fallos en la
ejecución de las funciones estadísticas como el cálculo de matrices de
covarianza, correlaciones y algoritmos de normalización es necesario
implementar una previa transformación. Este proceso consistie en
sustituir los separadores decimales, eliminar los caracteres
alfanuméricos y forzar la conversión de estas columnas a un formato
numérico continuo (float), asegurando así la integridad y la correcta
manipulación matemática de la información.

```{r}
# Esta funcion me permite trasformar mis datos para que ya no esten como porcentaje sino como decimal
limpiar_porcentajes <- function(x) {
  if (is.character(x)) {
    x <- as.numeric(gsub(",", ".", gsub("%", "", x))) / 100
  }
  return(x)
}

# Aplicar la conversión a todas las columnas de tasas y coberturas
datos_transformados <- datos %>%
  mutate(across(
    starts_with("TASA") | starts_with("COBERTURA") | starts_with("DESERCIÓN") | 
      starts_with("APROBACIÓN") | starts_with("REPROBACIÓN") | starts_with("REPITENCIA"),
    limpiar_porcentajes
  ))

```

Finalmente creamos un data set en el cual se efectuan todos los cambios
anteriores para asi poder tener datos con los cuales nos van a ser
posible trabajar y no tener problemas

```{r}
# Unificamos la exclusión de variables, la transformación numérica 
# y la eliminación de las filas con datos faltantes restantes.
educacion_limpio <- datos %>%
  # 1. Excluir variables no relevantes o con exceso de nulos
  select(-any_of(variables_a_excluir)) %>%
  # 2. Transformar porcentajes a decimales numéricos
  mutate(across(
    starts_with("TASA") | starts_with("COBERTURA") | starts_with("DESERCIÓN") | 
      starts_with("APROBACIÓN") | starts_with("REPROBACIÓN") | starts_with("REPITENCIA"),
    limpiar_porcentajes
  )) %>%
  # 3. Eliminar filas incompletas (Listwise deletion) ya que representan menos del 1%
  na.omit()

# Verificar las dimensiones finales del dataset limpio
dim(educacion_limpio)
```

# Análisis Univariado

#### 1. MUNICIPIO: Categórica

```{r}

datos_mun <- educacion_limpio %>%
  count(MUNICIPIO, sort = TRUE) %>%
  head(25) # Puedes ajustar este número si deseas ver más o menos

# Gráfico de barras de frecuencia
ggplot(datos_mun, aes(x = fct_reorder(MUNICIPIO, n), y = n)) +
  geom_col(fill = "#3498db", color = "white") +
  coord_flip() +
  theme_minimal() +
  labs(
    title = "Frecuencia de Aparición por Municipio",
    subtitle = "Top 25 de municipios con mayor registro en el dataset",
    x = "Municipio",
    y = "Cantidad de veces (Frecuencia)"
  ) +
  theme(
    axis.text.y = element_text(size = 8),
    plot.title = element_text(face = "bold", size = 14)
  )

```

El gráfico de barras muestra la frecuencia de aparición de los 25
municipios con mayor cantidad de registros en el dataset, permitiendo
identificar cuáles concentran la mayor cantidad de observaciones. La
presencia de datos sesgados indica que un pequeño grupo de municipios
concentra la mayoría de los registros, lo que puede requerir ponderación
para evitar desbalance en análisis comparativos.

#### 2. DEPARTAMENTO: Categórica

```{r}
ggplot(educacion_limpio, aes(x = fct_infreq(DEPARTAMENTO))) +
  geom_bar(fill = "#8e44ad", color = "white") +
  coord_flip() +
  theme_minimal() +
  labs(
    title = "Frecuencia de Registros por Departamento",
    subtitle = "Análisis univariado de la variable categórica DEPARTAMENTO",
    x = "Departamento",
    y = "Cantidad de Municipios (Frecuencia)"
  ) +
  theme(
    axis.text.y = element_text(size = 8, face = "bold"),
    plot.title = element_text(face = "bold", size = 14),
    panel.grid.minor = element_blank()
  )
```

El gráfico de barras ilustra la cantidad de registros o municipios
agrupados por cada departamento en orden de frecuencia. Un sesgo
pronunciado en esta variable refleja disparidades territoriales en la
recolección o en la concentración administrativa, lo que significa que
ciertos departamentos dominan el volumen de datos frente a otros
periféricos.

#### 3. POBLACIÓN_5_16: Numérica

##### histograma 

```{r}
# Histograma filtrando poblaciones menores a 50,000
educacion_limpio %>%
  filter(POBLACIÓN_5_16 < 50000) %>%
  ggplot(aes(x = POBLACIÓN_5_16)) +
  geom_histogram(bins = 30, fill = "seagreen", color = "white") +
  labs(
    title = "Distribución de la Población (Municipios < 50k hab)",
    x = "Población (5-16 años)",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r }
# Boxplot con filtro
educacion_limpio %>%
  filter(POBLACIÓN_5_16 < 50000) %>%
  ggplot(aes(y = POBLACIÓN_5_16)) +
  geom_boxplot(fill = "tomato", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja (Municipios < 50k hab)",
    y = "Población (5-16 años)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

El histograma y el diagrama de caja (filtrados a menos de 50,000
habitantes) muestran una distribución fuertemente sesgada a la derecha,
evidenciando que la gran mayoría de los municipios tienen poblaciones
pequeñas o moderadas en este rango de edad. Los valores atípicos
excluidos corresponden a grandes ciudades metropolitanas, cuya alta
densidad demográfica sesgaría la visualización central si se incluyeran.

#### 4. TASA_MATRICULACIÓN_5_16: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(TASA_MATRICULACIÓN_5_16 >= 0 & TASA_MATRICULACIÓN_5_16 <= 1.25) %>%
  ggplot(aes(x = TASA_MATRICULACIÓN_5_16)) +
  geom_histogram(bins = 40, fill = "mediumseagreen", color = "white") +
  labs(
    title = "Distribución de la Tasa de Matriculación (5 a 16 años)",
    subtitle = "Excluyendo valores atípicos mayores a 1.25",
    x = "Tasa de Matriculación",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(TASA_MATRICULACIÓN_5_16 >= 0 & TASA_MATRICULACIÓN_5_16 <= 1.25) %>%
  ggplot(aes(y = TASA_MATRICULACIÓN_5_16)) +
  geom_boxplot(fill = "orange", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Tasa de Matriculación",
    subtitle = "Enfocado en el rango principal (0 a 1.25)",
    y = "Tasa de Matriculación (5-16 años)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

Las gráficas muestran la concentración de la tasa de matriculación entre
0 y 1.25, revelando que la mayoría de los municipios se sitúan en
niveles altos de cobertura escolar. La existencia de valores atípicos
superiores a 1.25 (a menudo producto de errores de registro o
matriculación intermunicipal) sesga la media hacia arriba, haciendo que
la mediana y el rango intercuartílico sean más confiables para evaluar
la tendencia central.

#### 5. COBERTURA_NETA: Numérica

##### histograma

```{r}
# Histograma filtrando valores atípicos (coberturas mayores a 1.25 o 125%)
educacion_limpio %>%
  filter(COBERTURA_NETA >= 0 & COBERTURA_NETA <= 1.25) %>%
  ggplot(aes(x = COBERTURA_NETA)) +
  geom_histogram(bins = 40, fill = "dodgerblue", color = "white") +
  labs(
    title = "Distribución de la Cobertura Neta",
    subtitle = "Excluyendo valores atípicos mayores a 1.25",
    x = "Cobertura Neta",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
# Boxplot enfocado en el rango principal
educacion_limpio %>%
  filter(COBERTURA_NETA >= 0 & COBERTURA_NETA <= 1.25) %>%
  ggplot(aes(y = COBERTURA_NETA)) +
  geom_boxplot(fill = "gold", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Cobertura Neta",
    subtitle = "Rango ajustado (0 a 1.25)",
    y = "Cobertura Neta"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

El histograma y el boxplot reflejan cómo se distribuye la proporción de
población en edad escolar matriculada adecuadamente en su respectivo
grado. Los datos sesgados o atípicos superiores al límite establecido
muestran inconsistencias estadísticas o casos excepcionales de atracción
estudiantil, afectando la simetría de la distribución y requiriendo un
análisis robusto sin estos extremos.

#### 6. COBERTURA_NETA_TRANSICIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_TRANSICIÓN >= 0 & COBERTURA_NETA_TRANSICIÓN <= 1.25) %>%
  ggplot(aes(x = COBERTURA_NETA_TRANSICIÓN)) +
  geom_histogram(bins = 40, fill = "orchid", color = "white") +
  labs(
    title = "Distribución de la Cobertura Neta en Transición",
    subtitle = "Excluyendo valores atípicos mayores a 1.25",
    x = "Cobertura Neta (Transición)",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_TRANSICIÓN >= 0 & COBERTURA_NETA_TRANSICIÓN <= 1.25) %>%
  ggplot(aes(y = COBERTURA_NETA_TRANSICIÓN)) +
  geom_boxplot(fill = "turquoise", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Cobertura Neta en Transición",
    subtitle = "Rango ajustado (0 a 1.25)",
    y = "Cobertura Neta (Transición)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

Permiten observar el comportamiento de la escolarización neta en la
primera infancia (transición), mostrando una concentración típica en
valores moderados. Los sesgos y valores atípicos detectados suelen
reflejar coberturas reportadas por encima del 100% debido a metodologías
de estimación poblacional imperfectas o ingreso adelantado de menores.

#### 7. COBERTURA_NETA_PRIMARIA: Numérica

#### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_PRIMARIA >= 0 & COBERTURA_NETA_PRIMARIA <= 1.25) %>%
  ggplot(aes(x = COBERTURA_NETA_PRIMARIA)) +
  geom_histogram(bins = 40, fill = "coral", color = "white") +
  labs(
    title = "Distribución de la Cobertura Neta en Primaria",
    subtitle = "Excluyendo valores atípicos mayores a 1.25",
    x = "Cobertura Neta (Primaria)",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_PRIMARIA >= 0 & COBERTURA_NETA_PRIMARIA <= 1.25) %>%
  ggplot(aes(y = COBERTURA_NETA_PRIMARIA)) +
  geom_boxplot(fill = "lightgreen", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Cobertura Neta en Primaria",
    subtitle = "Rango ajustado (0 a 1.25)",
    y = "Cobertura Neta (Primaria)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

Visualizan la proporción de niños en edad de primaria que cursan este
nivel correctamente. La presencia de asimetrías o datos atípicos sugiere
que algunos entes territoriales enfrentan retos severos de cobertura o
presentan distorsiones en el censo base, alterando la normalidad de la
distribución estadística.

#### 8. COBERTURA_NETA_SECUNDARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_SECUNDARIA >= 0 & COBERTURA_NETA_SECUNDARIA <= 1.25) %>%
  ggplot(aes(x = COBERTURA_NETA_SECUNDARIA)) +
  geom_histogram(bins = 40, fill = "slateblue", color = "white") +
  labs(
    title = "Distribución de la Cobertura Neta en Secundaria",
    subtitle = "Excluyendo valores atípicos mayores a 1.25",
    x = "Cobertura Neta (Secundaria)",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_SECUNDARIA >= 0 & COBERTURA_NETA_SECUNDARIA <= 1.25) %>%
  ggplot(aes(y = COBERTURA_NETA_SECUNDARIA)) +
  geom_boxplot(fill = "khaki", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Cobertura Neta en Secundaria",
    subtitle = "Rango ajustado (0 a 1.25)",
    y = "Cobertura Neta (Secundaria)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

Ilustran la concentración de la cobertura neta en secundaria, donde
usualmente se observa una caída respecto a primaria. Los valores
atípicos y sesgos señalan municipios con caídas drásticas por deserción
temprana o, por el contrario, sobreestimaciones en zonas urbanas
específicas.

#### 9. COBERTURA_NETA_MEDIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_MEDIA >= 0 & COBERTURA_NETA_MEDIA <= 1.25) %>%
  ggplot(aes(x = COBERTURA_NETA_MEDIA)) +
  geom_histogram(bins = 40, fill = "firebrick", color = "white") +
  labs(
    title = "Distribución de la Cobertura Neta en Educación Media",
    subtitle = "Excluyendo valores atípicos mayores a 1.25",
    x = "Cobertura Neta (Media)",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_NETA_MEDIA >= 0 & COBERTURA_NETA_MEDIA <= 1.25) %>%
  ggplot(aes(y = COBERTURA_NETA_MEDIA)) +
  geom_boxplot(fill = "pink", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Cobertura Neta en Educación Media",
    subtitle = "Rango ajustado (0 a 1.25)",
    y = "Cobertura Neta (Media)"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

Muestran la distribución de la cobertura neta en los grados superiores
de educación media, la cual suele presentar valores más bajos y
dispersos. La acumulación de datos hacia el extremo inferior y la
presencia de sesgos indican una transición deficiente hacia la educación
media en gran parte de los municipios analizados.

#### 10. COBERTURA_BRUTA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA >= 0 & COBERTURA_BRUTA <= 1.50) %>%
  ggplot(aes(x = COBERTURA_BRUTA)) +
  geom_histogram(bins = 40, fill = "darkcyan", color = "white") +
  labs(
    title = "Distribución de la Cobertura Bruta",
    subtitle = "Excluyendo valores atípicos mayores a 1.50",
    x = "Cobertura Bruta",
    y = "Frecuencia"
  ) +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA >= 0 & COBERTURA_BRUTA <= 1.50) %>%
  ggplot(aes(y = COBERTURA_BRUTA)) +
  geom_boxplot(fill = "plum", color = "black", alpha = 0.7) +
  labs(
    title = "Diagrama de Caja de la Cobertura Bruta",
    subtitle = "Rango ajustado (0 a 1.50)",
    y = "Cobertura Bruta"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_blank(), 
        axis.ticks.x = element_blank())
```

Reflejan la relación global de matriculados (incluyendo edades por fuera
del rango ideal) frente a la población teórica. Al permitir valores de
hasta 1.50, los gráficos capturan el fenómeno de la extraedad; los datos
sesgados hacia la derecha muestran que muchos municipios albergan
estudiantes rezagados en su ciclo escolar.

#### 11. COBERTURA_BRUTA_TRANSICIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_TRANSICIÓN >= 0 & COBERTURA_BRUTA_TRANSICIÓN <= 1.70) %>%
  ggplot(aes(x = COBERTURA_BRUTA_TRANSICIÓN)) +
  geom_histogram(bins = 30, fill = "hotpink", color = "white") +
  labs(title = "Histograma: Cobertura Bruta Transición", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_TRANSICIÓN >= 0 & COBERTURA_BRUTA_TRANSICIÓN <= 1.70) %>%
  ggplot(aes(y = COBERTURA_BRUTA_TRANSICIÓN)) +
  geom_boxplot(fill = "hotpink", alpha = 0.6) +
  labs(title = "Boxplot: Cobertura Bruta Transición", y = "Tasa") +
  theme_minimal() + theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Muestran la cobertura global en el nivel de transición, contemplando
niños de diversas edades. La presencia de valores atípicos elevados y
sesgos resalta distritos donde la demanda supera la población oficial
estimada, evidenciando retos de planificación o flujos migratorios
infantiles.

#### 12. COBERTURA_BRUTA_PRIMARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_PRIMARIA >= 0 & COBERTURA_BRUTA_PRIMARIA <= 1.50) %>%
  ggplot(aes(x = COBERTURA_BRUTA_PRIMARIA)) +
  geom_histogram(bins = 40, fill = "coral", color = "white") +
  labs(title = "Histograma: Cobertura Bruta en Primaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_PRIMARIA >= 0 & COBERTURA_BRUTA_PRIMARIA <= 1.50) %>%
  ggplot(aes(y = COBERTURA_BRUTA_PRIMARIA)) +
  geom_boxplot(fill = "lightgreen", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Cobertura Bruta en Primaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Describen la distribución de la cobertura bruta en primaria,
evidenciando el impacto de la extraedad y la repitencia acumulada. Los
sesgos gráficos señalan que una proporción importante de municipios
supera el 100% debido a alumnos mayores al rango tradicional cursando
estos grados.

#### 13. COBERTURA_BRUTA_SECUNDARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_SECUNDARIA >= 0 & COBERTURA_BRUTA_SECUNDARIA <= 1.50) %>%
  ggplot(aes(x = COBERTURA_BRUTA_SECUNDARIA)) +
  geom_histogram(bins = 40, fill = "slateblue", color = "white") +
  labs(title = "Histograma: Cobertura Bruta en Secundaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_SECUNDARIA >= 0 & COBERTURA_BRUTA_SECUNDARIA <= 1.50) %>%
  ggplot(aes(y = COBERTURA_BRUTA_SECUNDARIA)) +
  geom_boxplot(fill = "khaki", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Cobertura Bruta en Secundaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Las graficas de boxplot y el histograma permiten visualizar el
comportamiento de la matrícula total en secundaria en relación con la
cohorte de edad. Los datos atípicos y el sesgo de la distribución
identifican disparidades en la retención y el rezago escolar acumulado
en la adolescencia temprana.

#### 14. COBERTURA_BRUTA_MEDIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_MEDIA >= 0 & COBERTURA_BRUTA_MEDIA <= 1.50) %>%
  ggplot(aes(x = COBERTURA_BRUTA_MEDIA)) +
  geom_histogram(bins = 40, fill = "firebrick", color = "white") +
  labs(title = "Histograma: Cobertura Bruta en Media", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(COBERTURA_BRUTA_MEDIA >= 0 & COBERTURA_BRUTA_MEDIA <= 1.50) %>%
  ggplot(aes(y = COBERTURA_BRUTA_MEDIA)) +
  geom_boxplot(fill = "pink", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Cobertura Bruta en Media", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Los diagramas exponen la distribución de la cobertura bruta en educación
media, mostrando valores frecuentemente reducidos. La asimetría y la
presencia de valores atípicos altos revelan que el acceso a este ciclo
terminal es altamente heterogéneo y limitado en la mayoría de regiones.

#### 15. DESERCIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(DESERCIÓN >= 0 & DESERCIÓN <= 0.20) %>%
  ggplot(aes(x = DESERCIÓN)) +
  geom_histogram(bins = 40, fill = "gray40", color = "white") +
  labs(title = "Histograma: Deserción Total", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(DESERCIÓN >= 0 & DESERCIÓN <= 0.20) %>%
  ggplot(aes(y = DESERCIÓN)) +
  geom_boxplot(fill = "darkred", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Deserción Total", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Las gráficas anteriores muestran la concentración de la tasa de abandono
escolar total en rangos bajos (menores al 20%). Los datos sesgados hacia
la derecha y los valores atípicos identifican a los municipios críticos
con problemas graves de exclusión educativa, los cuales alejan la
distribución de una forma normal simétrica.

#### 16. DESERCIÓN_TRANSICIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_TRANSICIÓN >= 0 & DESERCIÓN_TRANSICIÓN <= 0.20) %>%
  ggplot(aes(x = DESERCIÓN_TRANSICIÓN)) +
  geom_histogram(bins = 40, fill = "gray50", color = "white") +
  labs(title = "Histograma: Deserción en Transición", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_TRANSICIÓN >= 0 & DESERCIÓN_TRANSICIÓN <= 0.20) %>%
  ggplot(aes(y = DESERCIÓN_TRANSICIÓN)) +
  geom_boxplot(fill = "indianred", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Deserción en Transición", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

los diagramas ilustran el comportamiento de la deserción en la etapa de
transición. Aunque la mayoría de los valores se concentran cerca de
cero, los sesgos y valores atípicos señalan focos específicos donde el
abandono temprano es inusual y requiere intervención prioritaria.

#### 17. DESERCIÓN_PRIMARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_PRIMARIA >= 0 & DESERCIÓN_PRIMARIA <= 0.20) %>%
  ggplot(aes(x = DESERCIÓN_PRIMARIA)) +
  geom_histogram(bins = 40, fill = "gray60", color = "white") +
  labs(title = "Histograma: Deserción en Primaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_PRIMARIA >= 0 & DESERCIÓN_PRIMARIA <= 0.20) %>%
  ggplot(aes(y = DESERCIÓN_PRIMARIA)) +
  geom_boxplot(fill = "salmon", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Deserción en Primaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Por lo anterior podemos visualizan la tasa de abandono escolar durante
la primaria. La asimetría positiva en el histograma indica que, si bien
la mayoría de municipios mantienen tasas bajas, existen casos atípicos
con deserciones elevadas que actúan como puntos de influencia anómalos.

#### 18. DESERCIÓN_SECUNDARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_SECUNDARIA >= 0 & DESERCIÓN_SECUNDARIA <= 0.25) %>%
  ggplot(aes(x = DESERCIÓN_SECUNDARIA)) +
  geom_histogram(bins = 40, fill = "gray70", color = "white") +
  labs(title = "Histograma: Deserción en Secundaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_SECUNDARIA >= 0 & DESERCIÓN_SECUNDARIA <= 0.25) %>%
  ggplot(aes(y = DESERCIÓN_SECUNDARIA)) +
  geom_boxplot(fill = "lightcoral", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Deserción en Secundaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Las anteriores reflejan la deserción en secundaria (con un filtro
ajustado hasta 0.25). Se observa un sesgo marcado hacia los valores
bajos, pero con una cola más alargada y valores atípicos que evidencian
el incremento crítico del abandono escolar en la adolescencia.

#### 19. DESERCIÓN_MEDIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_MEDIA >= 0 & DESERCIÓN_MEDIA <= 0.25) %>%
  ggplot(aes(x = DESERCIÓN_MEDIA)) +
  geom_histogram(bins = 40, fill = "gray80", color = "black") +
  labs(title = "Histograma: Deserción en Media", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(DESERCIÓN_MEDIA >= 0 & DESERCIÓN_MEDIA <= 0.25) %>%
  ggplot(aes(y = DESERCIÓN_MEDIA)) +
  geom_boxplot(fill = "tomato", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Deserción en Media", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Muestran la distribución del abandono escolar en la educación media. Los
datos sesgados y la presencia de valores atípicos remarcan que el riesgo
de deserción escolar se intensifica en los grados finales, afectando de
forma desigual a los municipios del país.

#### 20. APROBACIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(APROBACIÓN >= 0.50 & APROBACIÓN <= 1.0) %>%
  ggplot(aes(x = APROBACIÓN)) +
  geom_histogram(bins = 40, fill = "forestgreen", color = "white") +
  labs(title = "Histograma: Aprobación Total", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(APROBACIÓN >= 0.50 & APROBACIÓN <= 1.0) %>%
  ggplot(aes(y = APROBACIÓN)) +
  geom_boxplot(fill = "palegreen", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Aprobación Total", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Las gráficas muestran la concentración de la aprobación total en la
parte alta de la distribución (por encima del 50%). El sesgo negativo
(hacia la izquierda) indica que la gran mayoría de estudiantes aprueban
el año, mientras que los valores atípicos inferiores representan
municipios con crisis severas de rendimiento académico.

#### 21. APROBACIÓN_TRANSICIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_TRANSICIÓN >= 0.50 & APROBACIÓN_TRANSICIÓN <= 1.0) %>%
  ggplot(aes(x = APROBACIÓN_TRANSICIÓN)) +
  geom_histogram(bins = 40, fill = "olivedrab", color = "white") +
  labs(title = "Histograma: Aprobación en Transición", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_TRANSICIÓN >= 0.50 & APROBACIÓN_TRANSICIÓN <= 1.0) %>%
  ggplot(aes(y = APROBACIÓN_TRANSICIÓN)) +
  geom_boxplot(fill = "darkseagreen", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Aprobación en Transición", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Se observa en lo anterior el rendimiento en el nivel de transición,
donde las tasas de aprobación suelen ser extremadamente altas. Los
sesgos hacia el 100% y los pocos valores atípicos inferiores reflejan
que casi la totalidad de los niños superan esta etapa inicial sin
reprobar.

#### 22. APROBACIÓN_PRIMARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_PRIMARIA >= 0.50 & APROBACIÓN_PRIMARIA <= 1.0) %>%
  ggplot(aes(x = APROBACIÓN_PRIMARIA)) +
  geom_histogram(bins = 40, fill = "seagreen", color = "white") +
  labs(title = "Histograma: Aprobación en Primaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_PRIMARIA >= 0.50 & APROBACIÓN_PRIMARIA <= 1.0) %>%
  ggplot(aes(y = APROBACIÓN_PRIMARIA)) +
  geom_boxplot(fill = "mediumseagreen", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Aprobación en Primaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

El histograma y el boxplot muestran la distribución de la aprobación en
la escuela primaria. El fuerte sesgo hacia tasas elevadas refleja un
comportamiento generalizado de éxito académico, quedando los valores
atípicos bajos como excepciones asociadas a contextos de vulnerabilidad
extrema.

#### 23. APROBACIÓN_SECUNDARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_SECUNDARIA >= 0.50 & APROBACIÓN_SECUNDARIA <= 1.0) %>%
  ggplot(aes(x = APROBACIÓN_SECUNDARIA)) +
  geom_histogram(bins = 40, fill = "mediumaquamarine", color = "white") +
  labs(title = "Histograma: Aprobación en Secundaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_SECUNDARIA >= 0.50 & APROBACIÓN_SECUNDARIA <= 1.0) %>%
  ggplot(aes(y = APROBACIÓN_SECUNDARIA)) +
  geom_boxplot(fill = "aquamarine", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Aprobación en Secundaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Se puede observar el comportamiento de la aprobación en secundaria,
donde la distribución empieza a perder tanta concentración superior
debido a la mayor dificultad académica. Los datos atípicos inferiores
señalan municipios con tasas de éxito inusualmente bajas.

#### 24. APROBACIÓN_MEDIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_MEDIA >= 0.50 & APROBACIÓN_MEDIA <= 1.0) %>%
  ggplot(aes(x = APROBACIÓN_MEDIA)) +
  geom_histogram(bins = 40, fill = "lightseagreen", color = "white") +
  labs(title = "Histograma: Aprobación en Media", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(APROBACIÓN_MEDIA >= 0.50 & APROBACIÓN_MEDIA <= 1.0) %>%
  ggplot(aes(y = APROBACIÓN_MEDIA)) +
  geom_boxplot(fill = "turquoise", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Aprobación en Media", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Las anteriores graficas describen la tasa de aprobación en el ciclo de
educación media. La asimetría negativa y la dispersión en el diagrama de
caja muestran que el éxito escolar en este nivel es más variable y
sujeto a las condiciones socioeconómicas locales.

#### 25. REPROBACIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN >= 0 & REPROBACIÓN <= 0.25) %>%
  ggplot(aes(x = REPROBACIÓN)) +
  geom_histogram(bins = 40, fill = "orange", color = "white") +
  labs(title = "Histograma: Reprobación Total", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN >= 0 & REPROBACIÓN <= 0.25) %>%
  ggplot(aes(y = REPROBACIÓN)) +
  geom_boxplot(fill = "gold", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Reprobación Total", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Muestran la concentración de la tasa de reprobación total en valores
reducidos (menores al 25%). El sesgo positivo y la presencia de valores
atípicos altos revelan que el fracaso escolar es un fenómeno concentrado
en municipios específicos con rezagos estructurales.

#### 26. REPROBACIÓN_TRANSICIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_TRANSICIÓN >= 0 & REPROBACIÓN_TRANSICIÓN <= 0.25) %>%
  ggplot(aes(x = REPROBACIÓN_TRANSICIÓN)) +
  geom_histogram(bins = 40, fill = "darkorange", color = "white") +
  labs(title = "Histograma: Reprobación en Transición", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_TRANSICIÓN >= 0 & REPROBACIÓN_TRANSICIÓN <= 0.25) %>%
  ggplot(aes(y = REPROBACIÓN_TRANSICIÓN)) +
  geom_boxplot(fill = "navajowhite", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Reprobación en Transición", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Ilustran la reprobación en el grado de transición, la cual es
naturalmente marginal. Los datos atípicos y el sesgo acentuado muestran
que reprobar en este nivel es un evento excepcional que ocurre casi
exclusivamente en contextos muy específicos.

#### 27. REPROBACIÓN_PRIMARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_PRIMARIA >= 0 & REPROBACIÓN_PRIMARIA <= 0.25) %>%
  ggplot(aes(x = REPROBACIÓN_PRIMARIA)) +
  geom_histogram(bins = 40, fill = "chocolate", color = "white") +
  labs(title = "Histograma: Reprobación en Primaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_PRIMARIA >= 0 & REPROBACIÓN_PRIMARIA <= 0.25) %>%
  ggplot(aes(y = REPROBACIÓN_PRIMARIA)) +
  geom_boxplot(fill = "bisque", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Reprobación en Primaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Reflejan la distribución de la reprobación en primaria. La alta
concentración cerca del cero y los valores atípicos hacia la derecha
indican que el reproceso escolar afecta a una minoría de municipios con
problemas particulares de calidad o continuidad.

#### 28. REPROBACIÓN_SECUNDARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_SECUNDARIA >= 0 & REPROBACIÓN_SECUNDARIA <= 0.30) %>%
  ggplot(aes(x = REPROBACIÓN_SECUNDARIA)) +
  geom_histogram(bins = 40, fill = "sienna", color = "white") +
  labs(title = "Histograma: Reprobación en Secundaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_SECUNDARIA >= 0 & REPROBACIÓN_SECUNDARIA <= 0.30) %>%
  ggplot(aes(y = REPROBACIÓN_SECUNDARIA)) +
  geom_boxplot(fill = "peachpuff", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Reprobación en Secundaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Muestran el comportamiento de la reprobación en secundaria (filtrada
hasta 0.30). Los gráficos evidencian una mayor dispersión y un sesgo
menos abrupto que en primaria, lo que significa que la reprobación se
incrementa y se extiende a lo largo de más municipios adolescentes.

#### 29. REPROBACIÓN_MEDIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_MEDIA >= 0 & REPROBACIÓN_MEDIA <= 0.25) %>%
  ggplot(aes(x = REPROBACIÓN_MEDIA)) +
  geom_histogram(bins = 40, fill = "peru", color = "white") +
  labs(title = "Histograma: Reprobación en Media", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPROBACIÓN_MEDIA >= 0 & REPROBACIÓN_MEDIA <= 0.25) %>%
  ggplot(aes(y = REPROBACIÓN_MEDIA)) +
  geom_boxplot(fill = "wheat", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Reprobación en Media", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Podemos ver en las anteriores graficas la tasa de reprobación en
educación media. La presencia de sesgos y valores atípicos superiores
destaca que los estudiantes de los grados finales enfrentan mayores
riesgos de no superar las asignaturas, variando intensamente entre
regiones.

#### 30. REPITENCIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPITENCIA >= 0 & REPITENCIA <= 0.20) %>%
  ggplot(aes(x = REPITENCIA)) +
  geom_histogram(bins = 40, fill = "mediumpurple", color = "white") +
  labs(title = "Histograma: Repitencia Total", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPITENCIA >= 0 & REPITENCIA <= 0.20) %>%
  ggplot(aes(y = REPITENCIA)) +
  geom_boxplot(fill = "thistle", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Repitencia Total", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Se observa la distribución de la repitencia total en el sistema
educativo. El marcado sesgo hacia la izquierda (valores cercanos a cero)
y los valores atípicos superiores indican que repetir año es un problema
localizado en un grupo minoritario de municipios.

#### 31. REPITENCIA_TRANSICIÓN: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPITENCIA_TRANSICIÓN >= 0 & REPITENCIA_TRANSICIÓN <= 0.15) %>%
  ggplot(aes(x = REPITENCIA_TRANSICIÓN)) +
  geom_histogram(bins = 40, fill = "purple", color = "white") +
  labs(title = "Histograma: Repitencia en Transición", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPITENCIA_TRANSICIÓN >= 0 & REPITENCIA_TRANSICIÓN <= 0.15) %>%
  ggplot(aes(y = REPITENCIA_TRANSICIÓN)) +
  geom_boxplot(fill = "plum", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Repitencia en Transición", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Podemos ver que al estar fuertemente sesgada hacia el cero, la gráfica
demuestra que la repetición en este nivel es estadísticamente casi nula,
apareciendo solo como valores atípicos aislados en registros atípicos.

#### 32. REPITENCIA_PRIMARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPITENCIA_PRIMARIA >= 0 & REPITENCIA_PRIMARIA <= 0.20) %>%
  ggplot(aes(x = REPITENCIA_PRIMARIA)) +
  geom_histogram(bins = 40, fill = "darkviolet", color = "white") +
  labs(title = "Histograma: Repitencia en Primaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPITENCIA_PRIMARIA >= 0 & REPITENCIA_PRIMARIA <= 0.20) %>%
  ggplot(aes(y = REPITENCIA_PRIMARIA)) +
  geom_boxplot(fill = "orchid", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Repitencia en Primaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

El comportamiento de la repitencia en primaria. La asimetría positiva y
los puntos atípicos superiores señalan que, aunque la mayoría de
escuelas controlan este indicador, persisten municipios con tasas de
repetición alarmantes.

#### 33. REPITENCIA_SECUNDARIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPITENCIA_SECUNDARIA >= 0 & REPITENCIA_SECUNDARIA <= 0.25) %>%
  ggplot(aes(x = REPITENCIA_SECUNDARIA)) +
  geom_histogram(bins = 40, fill = "blueviolet", color = "white") +
  labs(title = "Histograma: Repitencia en Secundaria", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPITENCIA_SECUNDARIA >= 0 & REPITENCIA_SECUNDARIA <= 0.25) %>%
  ggplot(aes(y = REPITENCIA_SECUNDARIA)) +
  geom_boxplot(fill = "mediumorchid", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Repitencia en Secundaria", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

la distribución de la repitencia en secundaria. La mayor dispersión en
el diagrama de caja y el sesgo positivo indican que el fenómeno de
repetir curso se vuelve más frecuente y heterogéneo durante la
adolescencia.

#### 34. REPITENCIA_MEDIA: Numérica

##### histograma

```{r}
educacion_limpio %>%
  filter(REPITENCIA_MEDIA >= 0 & REPITENCIA_MEDIA <= 0.20) %>%
  ggplot(aes(x = REPITENCIA_MEDIA)) +
  geom_histogram(bins = 40, fill = "blue", color = "white") +
  labs(title = "Histograma: Repitencia en Media", x = "Tasa", y = "Frecuencia") +
  theme_minimal()
```

##### boxplot

```{r}
educacion_limpio %>%
  filter(REPITENCIA_MEDIA >= 0 & REPITENCIA_MEDIA <= 0.20) %>%
  ggplot(aes(y = REPITENCIA_MEDIA)) +
  geom_boxplot(fill = "violet", color = "black", alpha = 0.7) +
  labs(title = "Boxplot: Repitencia en Media", y = "Tasa") +
  theme_minimal() +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())
```

Describen la repitencia en la educación media. Los datos sesgados y los
valores atípicos reflejan que, aunque muchos municipios mantienen tasas
bajas, existen regiones donde la pérdida de años en la educación media
representa un reto de eficiencia institucional significativo.
